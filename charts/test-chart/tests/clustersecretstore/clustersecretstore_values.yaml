clustersecretstores:
  aws-store:
    name: aws-secretsmanager
    aws:
      region: us-east-1
      auth:
        jwt:
          serviceAccountRef:
            name: aws-sa
            namespace: test-namespace